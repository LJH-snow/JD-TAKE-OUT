import React from 'react';
import { Typography } from 'antd';
const { Title } = Typography;

const EmployeeManagement = () => {
  return <Title level={2}>员工管理</Title>;
};

export default EmployeeManagement;
